<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.input :description %>
<%= f.association :ingredient %>
<%= f.button :submit, "Add Ingredient", class: "btn btn-outline-success my-2 my-sm-0" %>
<% end %>
